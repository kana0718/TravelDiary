
<h1>Edit Profile</h1>
<h4>Name</h4>
<%= form_with model: @user do |f| %>
  <%= f.text_field :name %>

  <h4>Image</h4>
  <%= f.file_field :profile_image, accept: "image/*" %>

　<%= f.submit 'Update User' %>

<% end %>